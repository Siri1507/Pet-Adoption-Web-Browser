import React from 'react';
import ReactDOM from 'react-dom';
import Routes from './adopt-me/client/src/Routes'; // Import the Routes component

ReactDOM.render(
  <React.StrictMode>
    <Routes /> {/* Use the Routes component */}
  </React.StrictMode>,
  document.getElementById('root')
);
